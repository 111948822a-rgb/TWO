# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# FastAPI 处理 UploadFile / Form 表单所需
python-multipart>=0.0.9

# 数据模型与配置
pydantic>=2.6.0
pydantic-settings>=2.1.0

# 异步任务队列
celery>=5.3.0
redis>=5.0.0

# 数据库
sqlalchemy>=2.0.0

# 视频 / 音频处理
# ffmpeg-python: 滤镜图构建;imageio-ffmpeg: 自带 ffmpeg 二进制(免系统安装)
ffmpeg-python>=0.2.0
imageio-ffmpeg>=0.5.0

# HTTP 客户端(调用第三方 API)
httpx>=0.27.0

# LLM(DeepSeek 通过 openai 兼容库调用)
openai>=1.12.0
gunicorn>=22.0.0

# 抠图(白底图 -> 透明 PNG,供通义万相 background-generation 使用)
# 首次使用会自动下载 U2Net 模型;若不需要抠图可不装
rembg>=2.0.50

# V6.1 边缘后处理(去白边/Defringing): 形态学腐蚀 + 高斯羽化
# opencv-python-headless: cv2.erode / cv2.GaussianBlur(无 GUI 依赖,适合服务器)
opencv-python-headless>=4.8.0
# scipy: cv2 不可用时的降级方案(scipy.ndimage.binary_erosion)
scipy>=1.11.0

# 音频时长读取(纯 Python,无需 ffmpeg,读取 TTS 生成的 mp3 时长)
mutagen>=1.47.0

# 阿里云 OSS
oss2>=2.18.0

# 环境变量
python-dotenv>=1.0.0


