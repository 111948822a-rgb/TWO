# V17.0 极简登录系统
# JWT 签名密钥: 生产环境由 Render 自动生成(JWT_SECRET_KEY=generateValue:true)。
# 本地开发若不设置, 将回退到代码内置的开发密钥(仅用于本地测试, 非机密)。
# 如需本地自定义密钥, 可取消下行注释并替换为强随机串:
# JWT_SECRET_KEY=your-strong-random-secret-here

# 以下为原有业务配置示例(按需填写):
# DEEPSEEK_API_KEY=sk-xxx
# DASHSCOPE_API_KEY=sk-xxx
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=
# OSS_BUCKET_NAME=
